# Copy to .env.local for local development
VITE_API_BASE_URL=http://localhost:3001

# Optional: landing page "Pilot tier" card (defaults: $19/mo flat, no per-order line)
# Option A (usage): VITE_PILOT_MONTHLY_USD=0 and VITE_PILOT_PER_ORDER_USD=0.15
# VITE_PILOT_MONTHLY_USD=19
# VITE_PILOT_PER_ORDER_USD=0
